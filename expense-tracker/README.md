# Personal Expense Tracker

## Setup

Install dependencies:

```bash
npm install
```

Run locally:

```bash
npm run dev
```

Build project:

```bash
npm run build
```

## Deploy on Cloudflare Pages

Build command:
npm run build

Output directory:
dist
